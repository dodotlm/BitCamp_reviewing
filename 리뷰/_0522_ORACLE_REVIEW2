## JOIN

- FROM만으로는 한 개의 테이블만 가져올 수 있는데(서브쿼리 제외ㅋㅋ) JOIN을 사용하면 다수의 테이블을 끌고 올 수 있다.
- 보통 JOIN에 ON을 같이, 아니 필수적으로 쓴다. 

- INNER JOIN 
보통 흔히 쓰는 JOIN문이며 INNER는 생략하고 쓴다.

FROM 테이블A
(INNER) JOIN 테이블B 
ON 테이블A의 공통컬럼 = 테이블B의 공통컬럼

이런 구조로 이루어져 있고 테이블A와 테이블B의 
