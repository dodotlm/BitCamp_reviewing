## 람다식

- 객체 지향 언어의 특징을 가장 잘 살린 장치. Spring을 사용하게 된다면 필수적으로 알아야 하는 부분이다.

- 이래저래 얘기해도 어려울테니 람다식의 형태를 보면 잘 알 수 있게 될 것이다.

() -> { 실행문; 실행문; }
() -> 실행문;
(타입 매개변수) -> { 실행문; 실행문; }

보통은 이런 식으로 사용하곤 한다.

- 하지만 람다식은 인터페이스 변수에 대입된다. 즉 인터페이스의 익명 구현 객체를 생성한다. 즉 인터페이스를 이용할 때 절찬리에 사용되는 구문이기도 하다는 뜻이다.
